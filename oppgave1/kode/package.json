{
  "name": "regnskap-oppgave1-kpi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:data": "uv run python scripts/prepare_data.py",
    "validate": "uv run python scripts/validate_kpi_periods.py",
    "test:excel": "uv run python -m unittest tests.test_task1_excel_fasit tests.test_dashboard_detail_fasit -v",
    "test:excel-upload": "node --test tests/task1_excel_upload.test.js",
    "test:parquet": "node --test tests/task1_parquet.test.js",
    "test:upload-files": "uv run python -m unittest tests.test_upload_testdata -v",
    "test:storage": "node --test tests/task1_storage.test.js",
    "test:e2e": "playwright test",
    "sources": "evidence sources --strict",
    "build:web": "uv run python ../../scripts/clean_static_build.py . && env -u INIT_CWD -u npm_config_local_prefix evidence build:strict",
    "verify:production": "uv run python ../../scripts/verify_static_build.py oppgave1 . --profile internal",
    "build": "npm run prepare:data && npm run validate && npm run sources && npm run build:web && npm run verify:production",
    "audit:production": "npm audit --omit=dev",
    "refresh": "npm test && npm run sources && npm run build:web && npm run verify:production",
    "test": "npm run prepare:data && npm run test:excel && npm run test:excel-upload && npm run test:parquet && npm run test:upload-files && npm run test:storage && npm run sources && npm run test:e2e && npm run validate",
    "dev:fast": "env -u INIT_CWD -u npm_config_local_prefix evidence dev --host 0.0.0.0 --port 3003",
    "preview": "env -u INIT_CWD -u npm_config_local_prefix evidence preview"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "type": "module",
  "dependencies": {
    "@evidence-dev/core-components": "^5.4.2",
    "@evidence-dev/duckdb": "^2.0.1",
    "@evidence-dev/evidence": "^40.1.8",
    "hyparquet": "^1.28.1",
    "hyparquet-compressors": "^1.1.1",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "5.4.2"
  }
}
