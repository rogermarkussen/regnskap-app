{
  "name": "regnskap-oppgave3-workflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:data": "uv run python scripts/prepare_data.py",
    "validate": "uv run python scripts/validate_task3.py",
    "test:fasit": "uv run python -m unittest tests.test_task3_fasit tests.test_task3_rules -v",
    "sources": "evidence sources --strict",
    "build:web": "uv run python ../scripts/clean_static_build.py . && env -u INIT_CWD -u npm_config_local_prefix evidence build:strict",
    "verify:production": "uv run python ../scripts/verify_static_build.py oppgave3 . --profile internal",
    "build": "npm run prepare:data && npm run validate && npm run sources && npm run build:web && npm run verify:production",
    "refresh": "npm test && npm run sources && npm run build:web && npm run verify:production",
    "test": "npm run prepare:data && npm run validate && npm run test:fasit",
    "dev:fast": "env -u INIT_CWD -u npm_config_local_prefix evidence dev --host 0.0.0.0 --port 3002",
    "preview": "env -u INIT_CWD -u npm_config_local_prefix evidence preview"
  },
  "engines": { "node": ">=22 <23" },
  "type": "module",
  "dependencies": {
    "@evidence-dev/core-components": "^5.4.2",
    "@evidence-dev/duckdb": "^2.0.1",
    "@evidence-dev/evidence": "^40.1.8"
  },
  "devDependencies": {
    "typescript": "5.4.2"
  }
}
