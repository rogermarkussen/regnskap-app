{
  "name": "regnskap-oppgave3-web",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "prepare:data": "uv run python scripts/prepare_data.py",
    "export:web": "uv run python scripts/export_web_data.py",
    "validate": "uv run python scripts/validate_task3.py",
    "test:fasit": "uv run python -m unittest tests.test_task3_fasit tests.test_task3_rules -v",
    "build:web": "uv run python ../scripts/clean_static_build.py . && vite build",
    "build:public:web": "python ../scripts/clean_static_build.py . && PUBLIC_BUILD=true vite build",
    "verify:public": "python ../scripts/verify_static_build.py oppgave3 . --profile public",
    "build:public": "npm run build:public:web && npm run verify:public",
    "verify:production": "uv run python ../scripts/verify_static_build.py oppgave3 . --profile internal",
    "build": "npm run prepare:data && npm run validate && npm run export:web && npm run build:web && npm run verify:production",
    "refresh": "npm test && npm run export:web && npm run build:web && npm run verify:production",
    "test": "npm run prepare:data && npm run validate && npm run test:fasit",
    "dev:fast": "npm run export:web && vite --host 0.0.0.0 --port 3002",
    "preview": "vite preview --host 0.0.0.0 --port 3002"
  },
  "engines": { "node": ">=22" },
  "type": "module",
  "dependencies": {
    "hyparquet": "^1.28.1",
    "hyparquet-compressors": "^1.1.1",
    "svelte": "5.56.10"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "vite": "8.2.2"
  }
}
